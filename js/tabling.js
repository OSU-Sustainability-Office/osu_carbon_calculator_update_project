/* Open when someone clicks on the span element */
function openSOWeb() {
    document.getElementById("so_web").style.height = "100%";
}

/* Close when someone clicks on the "x" symbol inside the overlay */
function closeSOWeb() {
    document.getElementById("so_web").style.height = "0%";
}
